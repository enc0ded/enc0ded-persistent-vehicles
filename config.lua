Config = {
  runEvery = 3, -- in seconds. Anywhere between (2-10) is good.
  populateOnReboot = true, -- populate server vehicles after server reboot
  debug = false, -- show updates in the console
}
